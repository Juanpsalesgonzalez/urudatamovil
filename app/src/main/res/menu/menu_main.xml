<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/menu_main"
    tools:context="com.proyecto.urudatamobile.DisplayMessageActivity">


    <item
        android:title="@string/menu_action_close"
        android:id="@+id/menu_main_action_close"
        android:icon="@drawable/btn_close_normal"
        app:showAsAction="always"
        android:visible="true"
        android:enabled="true"/>
    <item
        android:title="Opciones"
        android:id="@+id/menu_main_opciones"
        android:icon="@drawable/ic_action"
        app:showAsAction="always"
        android:visible="true"
        android:onClick="showPopup"
        android:enabled="true"/>
</menu>

